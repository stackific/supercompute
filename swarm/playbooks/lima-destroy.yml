- name: Destroy the template-local Lima cluster
  hosts: localhost
  gather_facts: true
  environment:
    LIMA_HOME: "{{ lima_runtime_home }}"
    LIMA_SSH_PORT_FORWARDER: "false"
  roles:
    - role: lima
      vars:
        lima_lifecycle: absent
