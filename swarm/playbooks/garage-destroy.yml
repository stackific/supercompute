---
- name: Destroy only standalone Docker Swarm Garage resources
  hosts: localhost
  gather_facts: false
  environment:
    DOCKER_HOST: "{{ garage_docker_host }}"
  vars:
    garage_lifecycle: destroy
  roles:
    - garage
