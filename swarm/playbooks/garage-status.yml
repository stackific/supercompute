---
- name: Show standalone template-local Garage status
  hosts: localhost
  gather_facts: false
  environment:
    DOCKER_HOST: "{{ garage_docker_host }}"
  vars:
    garage_lifecycle: status
  roles:
    - garage
