---
- name: Start standalone template-local Garage in Lima's shared default profile
  hosts: localhost
  gather_facts: false
  environment:
    DOCKER_HOST: "{{ garage_docker_host }}"
  vars:
    garage_lifecycle: up
  roles:
    - garage
