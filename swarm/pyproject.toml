[project]
name = "docker-swarm"
version = "0.1.0"
description = "Reusable pinned Ansible template for three-manager Docker Swarm deployments"
requires-python = ">=3.13,<3.14"
dependencies = [
  "ansible-core==2.20.7",
  "cryptography==50.0.0",
]

[tool.uv]
package = false
