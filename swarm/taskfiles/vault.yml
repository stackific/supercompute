version: '3'

tasks:
  init:
    deps: [':bootstrap:sync']
    cmds:
      - bash "{{.ROOT_DIR}}/scripts/vault-init.sh" "{{.PROVIDER}}"

  edit:
    deps: [':bootstrap:sync']
    cmds:
      - bash "{{.ROOT_DIR}}/scripts/vault-edit.sh" "{{.PROVIDER}}"
