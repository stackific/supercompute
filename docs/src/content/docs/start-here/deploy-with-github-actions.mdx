---
title: Deploy with GitHub Actions
description: Bring up a production cluster from GitHub Actions
---

import { Tabs, TabItem } from "@astrojs/starlight/components";
import { Kbd } from "starlight-kbd/components";
import { Image } from "astro:assets";

This guide configures the **prod** environment so GitHub Actions runs deploy and verify—not your Mac. Use it when you want CI-driven lifecycle and do not need to permanently rely on Mac. CI uses the GitHub Actions → **Deploy** workflow, not `task up` on your Mac.

| Host | Role |
| --- | --- |
| **static-1** | Ubuntu 26.04 public endpoint (mesh hub, `x86_64`) |
| **static-2** | Second Ubuntu 26.04 public endpoint (`x86_64`) |

:::note[Prerequisites]
Finish [Get started locally](/start-here/get-started/) first (`task setup` complete). You need **two** Ubuntu 26.04 amd64 VMs with stable public IPs, an external Postgres instance with owner role hosted outside of the Supercompute cloud, and the committed **`inventories/prod/`** inventory with `control_plane: gha`. Do **not** run `task up` on your Mac for prod once you choose the GHA control plane.
:::

### Mac path vs GHA path

The same prod inventory works from your Mac or from GitHub Actions—the control plane differs.

Both paths use the same **`prod`** inventory shape. They differ by **control plane**.

| | Mac | GitHub Actions |
| --- | --- | --- |
| Guide | [Deploy to production](/start-here/deploy-to-production/) | This guide |
| Entry | `task up ENV=prod` | Actions → **Deploy** workflow |
| Who runs Ansible | Your Mac | GitHub-hosted ephemeral runner at `node_ci_address` (e.g. `.254`) |
| Permanent mesh peer | Mac at `node_controller_address` (usually `.1`) | None |
| Lima guests | Yes | Unsupported |

Keep **`dev`** for local Mac development. Use **`prod`** for production—either from your Mac or from GitHub Actions, not both at once on the same inventory.

### Step 1: Configure prod hosts

Set the `sc_*` FQDNs, then fill public IPs and SSH fingerprints for both static nodes.

For **static-1** and **static-2**, follow [Adding a public node](/guides/adding-public-node/) using the prod key (`~/.ssh/example-prod`) with an **empty passphrase**—the runner cannot unlock a passphrase-protected key. When you open TCP **22**, allow GitHub-hosted runner egress as well as your Mac `/32` (runner IPs change). Then come back here.

Fill `inventories/prod/hosts.yml` the same way as [Deploy to production — Step 1](/start-here/deploy-to-production/#step-1-configure-prod-hosts).

### Step 2: Configure DNS

Create records matching `sc_ns`, `sc_api`, `sc_app`, and `sc_apps` from Step 1. Leave `example.com` at your registrar. If the DNS is hosted on Cloudflare, do not enable the proxy orange icons.

| Inventory var | Example | Type | Value |
| --- | --- | --- | --- |
| `sc_ns` | `ns.example.com` | A | **static-1** public IPv4 |
| `sc_ns` | `ns.example.com` | A | **static-2** public IPv4 |
| `sc_api` | `sc-api.example.com` | CNAME | `ns.example.com` |
| `sc_app` | `sc-app.example.com` | CNAME | `ns.example.com` |
| `sc_apps` | `sc-apps.example.com` | NS | `ns.example.com` |

Supercompute's Nameserver then serves `*.sc-apps.example.com`.

To use different names, edit those `all.vars` in `hosts.yml`, create matching DNS records, and re-run `task up`.

### Step 3: Set GHA control plane

Tell the inventory that GitHub Actions—not your Mac—will run deploys.

Edit `inventories/prod/group_vars/all/main.yml` and set:

```yaml
control_plane: gha
```

### Step 4: Initialize the vault

Create the encrypted vault locally and back up the password for CI.

Initialize vault locally—even though deploys run in CI, you create secrets from a trusted machine:

```sh
task vault-init ENV=prod
```

Creates `inventories/prod/.vault-pass` and encrypted `vault.yml` with `vault_meta`, placeholder `vault_database_url`, and auto-generated `vault_database_secret`. The Deploy workflow's `ensure-secrets` step adds WireGuard keypairs on first `up`.

Decrypted `inventories/prod/group_vars/all/vault.yml` looks like this:

<Tabs syncKey="vault-init">
<TabItem label="prod">

```yaml
---
vault_meta:
  project: example
  provider: prod
vault_database_url: postgresql://app:secret@db.example.com:5432/supercompute
vault_database_secret: mT4nQ8pL2vK6wB9xF1hJ5cY0aZ3dE7gH_rXv
```

</TabItem>
<TabItem label="Template">

```yaml
---
vault_meta:
  project: <project>
  provider: <env>
vault_database_url: postgresql://REPLACE_WITH_USER:PASSWORD@HOST:5432/DATABASE
vault_database_secret: <auto-generated at vault-init>
```

</TabItem>
</Tabs>

Keep `inventories/prod/.vault-pass` **out of git** (store the same file's contents in GitHub repository secret `ANSIBLE_VAULT_PASSWORD`).

#### PostgreSQL database

Point the vault at your external Postgres before committing it for CI.

Supercompute needs a Postgres database with owner role hosted outside of the Supercompute cloud.

```sh
task vault-edit ENV=prod
```

`task vault-edit` decrypts `inventories/prod/group_vars/all/vault.yml` in your editor. You should see `vault_meta`, `vault_database_url`, and `vault_database_secret` (WireGuard keys appear after the first `task up`). Replace only `vault_database_url` with your connection URI (same shape as the **prod** tab). Leave everything else unchanged.

Example after you set the database URL:

<Tabs syncKey="postgres">
<TabItem label="prod">

```yaml
---
vault_meta:
  project: example
  provider: prod
vault_database_url: postgresql://app:secret@db.example.com:5432/supercompute
vault_database_secret: mT4nQ8pL2vK6wB9xF1hJ5cY0aZ3dE7gH_rXv
```

</TabItem>
<TabItem label="Template">

```yaml
---
vault_meta:
  project: <project>
  provider: <env>
vault_database_url: postgresql://REPLACE_WITH_USER:PASSWORD@HOST:5432/DATABASE
vault_database_secret: <auto-generated at vault-init>
```

</TabItem>
</Tabs>

Save and exit the editor:

- **vim/vi** (default on macOS): press <Kbd mac="Esc" windows="Esc" />, type `:wq`, press <Kbd mac="Enter" windows="Enter" />
- **nano**: press <Kbd mac="Control+O" windows="Control+O" />, <Kbd mac="Enter" windows="Enter" />, then <Kbd mac="Control+X" windows="Control+X" />

Deploy workflow validation rejects the vault-init placeholder in `vault_database_url`.

Commit the encrypted vault; CI decrypts with `ANSIBLE_VAULT_PASSWORD` (no separate database URL GitHub secret).

Commit `inventories/prod/hosts.yml`, `inventories/prod/group_vars/all/main.yml` (with `control_plane: gha`), and encrypted `inventories/prod/group_vars/all/vault.yml` in the repository where Actions runs. Never commit `.vault-pass`.

### Step 5: Repository secrets

Add the vault password and operator SSH key to GitHub Actions secrets.

In GitHub → **Settings** → **Secrets and variables** → **Actions**, add:

| Secret | Required | Value |
| --- | --- | --- |
| `ANSIBLE_VAULT_PASSWORD` | yes | Exact contents of `inventories/prod/.vault-pass` |
| `OPS_SSH_PRIVATE_KEY` | yes | Full private key file (same as `~/.ssh/example-prod`; must be passphrase-less) |
| `MAC_OPERATOR_SSH_PUBLIC_KEY` | no | Your Mac Ed25519 public key; if set, installed into `ops` authorized_keys on nodes |

Every node must allow passwordless `sudo` for `ops` (the runner cannot answer a become password).

### Step 6: Run the workflow

Trigger the Deploy workflow to bring up production from CI.

1. GitHub → **Actions** → **Deploy** → **Run workflow**.
2. **env**: `prod`.
3. **action**: `up`, `verify`, or `down`.
4. For **down**, set **confirm** to exactly `down-prod`.

<div class="not-content" style="display: flex; justify-content: center;">
  <Image
    src="/run-workflow.jpg"
    alt="GitHub Actions Run workflow dialog for the Deploy workflow, with env, action, and confirm fields."
    width={321}
    height={441}
    layout="fixed"
    style="width: 321px; height: auto; max-width: 100%;"
  />
</div>

**`up`** brings up the cluster using an ephemeral runner which is discarded once the cluster is up.

### Step 7: Verify

Confirm the workflow succeeded and nodes are reachable.

Re-run the workflow with **action** `verify`, or after an `up` job completes, SSH from your Mac if you installed `MAC_OPERATOR_SSH_PUBLIC_KEY`:

```sh
task ssh ENV=prod NODE=static-1
```

(Only works once your Mac can reach the mesh—typically after a successful `up` and if you rely on public bootstrap or an operator key on nodes.)

### Do not mix control planes

- **Do not** run `task up ENV=prod` on your Mac after setting `control_plane: gha`.
- **Do not** run the Deploy workflow against **`dev`** (`control_plane: mac`) unless you understand it skips the Mac peer.
- Pick **one** control plane for **`prod`**: Mac ([Deploy to production](/start-here/deploy-to-production/)) **or** GHA (this guide), not both.

### Checklist

| Item | Done |
| --- | --- |
| `control_plane: gha` set in `inventories/prod/group_vars/all/main.yml` | |
| `sc_*` FQDNs set in `hosts.yml` | |
| Parent DNS: `sc_ns` A per static + `sc_api`/`sc_app` CNAME to `sc_ns` + `sc_apps` NS (Cloudflare proxy off) | |
| `static-1` + `static-2` public IPs + fingerprints in `hosts.yml` | |
| `~/.ssh/example-prod` created (empty passphrase); public key on both statics | |
| `ssh … true` works for both static IPs | |
| `vault_database_url` set in encrypted `vault.yml`; `vault_database_secret` from `vault-init` | |
| GitHub secrets `ANSIBLE_VAULT_PASSWORD` + `OPS_SSH_PRIVATE_KEY` set | |
| Workflow **up** completed successfully | |

### Backup

Store off-repo securely: 

- `~/.ssh/example-prod` (+ `.pub`)
- `inventories/prod/.vault-pass`
- `inventories/prod/group_vars/all/vault.yml`