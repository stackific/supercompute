---
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<article class="sl-flex" aria-labelledby="about-section-heading">
	<small id="about-section-heading">{title}</small>
	<slot />
</article>

<style>
	article {
		max-width: 40rem;
		margin-inline: auto;
		padding-block: 5rem;
		flex-direction: column;
		align-items: center;
		text-align: center;
		gap: 0.5rem;
	}
	article > :global(*) {
		max-width: 50ch;
		margin-top: 0 !important;
	}
	small {
		color: var(--sl-color-gray-3);
	}
</style>
