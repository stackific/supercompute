---
# Remote-only connection settings. localhost must never inherit this contract.
ansible_host: "{{ cloud_wireguard_address }}"
wireguard_node_address: "{{ cloud_wireguard_address }}"

ansible_connection: "{{ cloud_connection }}"
ansible_user: >-
  {{
    (node_lima_guest | default(false) | bool)
    | ternary(lima_guest_user, cloud_default_ssh_user)
  }}
ansible_port: "{{ cloud_default_ssh_port }}"
ansible_become: true
ansible_become_method: sudo
ansible_ssh_private_key_file: >-
  {{
    (node_lima_guest | default(false) | bool)
    | ternary(lima_home_dir ~ '/_config/user', cloud_ssh_private_key_file)
  }}
ansible_ssh_common_args: >-
  -F /dev/null -o BatchMode=yes -o ControlMaster=no -o ControlPath=none
  -o HostKeyAlias={{ inventory_hostname }} -o StrictHostKeyChecking=yes
  -o UserKnownHostsFile={{ cloud_known_hosts }}
  -o IdentitiesOnly=yes
