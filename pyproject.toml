[project]
name = "cloud-deploy"
version = "0.1.0"
description = "Ansible deployment project"
requires-python = ">=3.14,<3.15"
dependencies = [
  "ansible-core==2.21.3",
  "pyyaml==6.0.3",
]

[tool.uv]
package = false
