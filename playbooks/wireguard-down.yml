---
- ansible.builtin.import_playbook: classify-mesh.yml

- name: Assert WireGuard-down preconditions after mesh classification
  hosts: localhost
  gather_facts: false
  any_errors_fatal: true
  tasks:
    - name: Require a public-endpoint provider for WireGuard teardown
      ansible.builtin.assert:
        that:
          - provider.platform == 'public'
          - provider.mode == 'remote'
        fail_msg: >-
          wireguard-down requires provider.platform=public for
          ENV={{ inventory_slug }}.

- ansible.builtin.import_playbook: node-transport.yml

- name: Tear down WireGuard on every server through mesh or bootstrap SSH
  hosts: nodes
  gather_facts: false
  any_errors_fatal: true
  serial: 1
  vars_files:
    - vars/node_recovery_ssh.yml
  roles:
    - role: wireguard_node
      vars:
        wireguard_lifecycle: absent
