---
- name: Restart PowerDNS
  ansible.builtin.systemd:
    name: "{{ cluster_pdns_service }}"
    state: restarted
    daemon_reload: true
  become: true

- name: Reload Caddy
  ansible.builtin.systemd:
    name: "{{ cluster_caddy_service }}"
    state: reloaded
  become: true
